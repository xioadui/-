<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.dao.IIndustryDataDao">
    <insert id="save" parameterType="java.util.List">
        insert ignore into industrydata(id,type,title,date,content,digest)
        values
        <foreach collection="info" item="item" index= "index" separator =",">
            (
            #{item.id,jdbcType=BIGINT},
            #{item.type,jdbcType=VARCHAR},
            #{item.title,jdbcType=VARCHAR},
            #{item.date,jdbcType=DATE},
            #{item.content,jdbcType=VARCHAR},
            #{item.digest,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>
    <select id="queryByTitle" resultType="com.enterprise.entity.IndustryData">
      SELECT id,type,title, date, content, digest
        FROM industrydata
<<<<<<< HEAD
        where title like #{title,jdbcType=VARCHAR};
        order by date desc
        limit #{index},#{length};
    </select>
    <select id="getAllInd" resultType="com.enterprise.entity.IndustryData">
         SELECT id,type,title, date, content, digest
        FROM industrydata
        order by date desc
=======
        where title like #{title,jdbcType=VARCHAR}
        order by date asc
>>>>>>> 072f66b0cd66e41ea585c03761c2198c980afe10
        limit #{index},#{length};
    </select>

    <select id="queryByDate" resultType="com.enterprise.entity.IndustryData">
         SELECT id,type,title, date, content, digest
        FROM industrydata
        where date = #{date}
        order by date desc
        limit #{index},#{length};
    </select>

    <select id="queryByType" resultType="com.enterprise.entity.IndustryData">
         SELECT id,type,title, date, content, digest
        FROM industrydata
        where type = #{type}
        order by date desc
        limit #{index},#{length};
    </select>

    <select id="searchIndustryData" resultType="com.enterprise.entity.IndustryData">
         SELECT id,type,title, date, content, digest
        FROM industrydata
        where type like #{condition} or title like #{condition}
        order by date desc
        limit #{index},#{length};
    </select>

    <select id="queryIndById" resultType="com.enterprise.entity.IndustryData">
      SELECT id,type,title, date, content, digest
        FROM industrydata where id = #{id,jdbcType=INTEGER};
    </select>

</mapper>