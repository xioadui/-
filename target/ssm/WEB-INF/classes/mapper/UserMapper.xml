<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.dao.IUserDao">
    <!--<resultMap id="BaseResultMap" type="com.kk.ssm.entity.User">
        <id column="usernumber" property="userNumber" jdbcType="INTEGER"/>
        <result column="username" property="userName" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>-->

    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="login" resultType="com.enterprise.entity.User">
        <!-- 具体的sql -->
        SELECT userId, userName, password, identity
        FROM user
        WHERE userId = #{userId,jdbcType=INTEGER} AND password = #{password, jdbcType=VARCHAR}
    </select>

    <insert id="register" parameterType="com.enterprise.entity.User">
        INSERT INTO user (userName, password, identity)
        VALUES(#{user.userName,jdbcType=VARCHAR}, #{user.password, jdbcType=VARCHAR}, #{user.identity,jdbcType=INTEGER});
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="user.userId">
            SELECT LAST_INSERT_ID() AS userId
        </selectKey>
    </insert>

</mapper>